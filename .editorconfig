root = true

# 通用设置（适用于所有文件）
[*]
charset = utf-8                  # 文件编码
end_of_line = lf                 # 换行符(LF)
indent_style = space             # 缩进风格(spaces/tabs)
indent_size = 2                  # 缩进空格数
trim_trailing_whitespace = true  # 删除行尾空格
insert_final_newline = true      # 文件末尾保留空行
